sunflower.init("Window Name!", 1000, 1000)
print("You can oviously do any lua codes")
sunflower.graphics.color(255, 100, 50, 255)

-- Simply makes it so when the app crashes the cmd only crashes on a key input
sunflower.debugger.pauseOnCrash = true